import React from "react";

export const HeroPageTwo = () => {
  return (
    <div className="hero-page two">
      <blockquote>
        Perksy helps you reach the right people, so can make decisions with more
        confidence.
      </blockquote>
    </div>
  );
};
