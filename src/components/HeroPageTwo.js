import React from "react";

export const HeroPageTwo = ({ clname }) => {
  return (
    <div className={`hero-page ${clname}`}>
      <blockquote>
        Perksy helps you reach the right people, so can make decisions with more
        confidence.
      </blockquote>
    </div>
  );
};
